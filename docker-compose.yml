services:
  app:
    container_name: front-mapa
    image: front-mapa-municipal:14.0
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
  api:
    container_name: api-mapa
    image: api-mapa-municipal:7.0
    restart: unless-stopped